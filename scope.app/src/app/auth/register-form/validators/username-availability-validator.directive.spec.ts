import { UsernameAvailabilityValidatorDirective } from './username-availability-validator.directive';

describe('UsernameAvailabilityValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameAvailabilityValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
